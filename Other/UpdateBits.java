public class UpdateBits {	/**     *@param n, m: Two integer     *@param i, j: Two bit positions     *return: An integer     */    public int updateBits(int n, int m, int i, int j) {        // way to set one big in n to the bit in m:        // 1. find the bit        // 2. set the bit in n to 0, through &        // 3. set the bit to the bit in m through |        for(int k = i; k <= j; k++){            // set the bit in n to 0            n = n & (~ (1 << k));            // set that bit to the bit in m            n = n | (((m >> (k - i)) & 1) << k);        }        return n;    }}