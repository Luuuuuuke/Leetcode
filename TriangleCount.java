import java.util.Arrays;/** * @param S: A list of integers * @return: An integer * @description: The basic idea is transforming to a 2 sum problem! */public class TriangleCount {    public int triangleCount(int S[]) {        if(S.length < 3) return 0;        // O(nlogn)        Arrays.sort(S);        int count = 0;        int i, low, high;        // O(n^2)        for(i = 2; i < S.length; i++){            low = 0; high = i - 1;            while(low < high){                if(S[low] + S[high] > S[i]){                    count += (high - low);                    high--;                } else low++;            }        }        return count;    }}