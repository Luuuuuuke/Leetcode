import java.util.*;public class EncodeDecodeString {	 // Encodes a list of strings to a single string.    public String encode(List<String> strs) {        // idea: when append a str, also appen its length, so append len/str        if(strs == null || strs.size() == 0){            return "";        }        StringBuilder sb = new StringBuilder();        for(String str : strs){            sb.append(str.length());            sb.append("/");            sb.append(str);        }        return sb.toString();    }    // Decodes a single string to a list of strings.    public List<String> decode(String s) {        List<String> res = new ArrayList<String>();        // idea: find each "/" after index i,        if(s == null || s.length() == 0){            return res;        }        int start = 0;        while(start < s.length()){            // after start, look for "/"            int slash = s.indexOf("/", start);            int len = Integer.parseInt(s.substring(start, slash));            res.add(s.substring(slash + 1,  slash + 1 + len));            start = slash + 1 + len;        }        return res;    }}